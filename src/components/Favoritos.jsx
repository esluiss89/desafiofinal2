/**
 * Componente de React que muestra la sección de Favoritos.
 * 
 * @returns {JSX.Element} Componente de React que renderiza la sección de Favoritos.
 */
import React from 'react';

const Favoritos = () => {
  return (
    <div>Favoritos</div>
  );
}

export default Favoritos;